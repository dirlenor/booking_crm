## Time & Pricing Logic
- Identified that  is the source of truth for runtime, while  is a template.
- Found that  was being incorrectly multiplied by pax in some areas of the code (like  logic).
- Established clear rules for Tier-based vs Flat-rate pricing calculations.
## Time & Pricing Logic
- Identified that `trips.time` is the source of truth for runtime, while `options.times` is a template.
- Found that `flatRatePrice` was being incorrectly multiplied by pax in some areas of the code (like `local-cart.ts` logic).
- Established clear rules for Tier-based vs Flat-rate pricing calculations.
